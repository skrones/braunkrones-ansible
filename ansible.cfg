[defaults]
inventory = ./inventory
remote_user = devops
host_key_checking = False
forks = 10
roles_path = ./roles

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
